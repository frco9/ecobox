<% content_for :head_title do %>
  Capteurs
<% end %>

<section class="row">
  <div class="sec_header col-md-12">
    <h2>Liste des capteurs</h2>
  </div>
  <div class="sec_content col-md-12">
    <%= render 'layouts/flash' %>
    <% @uniq_sensors.each_slice(3) do |s_sensors| %>
    <div class="row">
      <% s_sensors.each do |sensor| %>
        <div class="col-md-4">
          <div style="background-color: #FFF; padding: 15px; width: 100%;"> 
            <%= link_to sensor, :remote => true do %>
              <span><%= sensor.name %></span>
              <span><%= sensor.room.name if sensor.room %></span>
            <% end %>
            <% if current_user.admin? %> 
              <%= link_to "", sensor, remote: true, method: :delete, data: { confirm: "Etes vous certain de vouloir supprimer le capteur?" }, class: "glyphicon glyphicon-remove-circle pull-right", style: "font-size:14pt; margin-left:5px;" %>
              <%= link_to "", edit_sensor_path(sensor), remote: true, class: "glyphicon glyphicon-edit pull-right", style: "font-size:14pt; margin-left:5px;" %>
            <% end %>
            <%= link_to "", sensor, remote: true, class: "glyphicon glyphicon-cog pull-right", style: "font-size:14pt;" %>
            <span class="clearfix"></span>
          </div>  
          <div id="ajax_content_<%= sensor.id %>" style="margin-bottom: 15px;"></div>
        </div>
      <% end %>
    </div>
    <% end %>
  </div>
</section>
  


    <!-- <table class="table table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Frequency</th>
          <th>Modulation</th>
          <th>Room</th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @sensors.each do |sensor| %>
          <tr>
            <td><%= sensor.name %></td>
            <td><%= sensor.frequency %></td>
            <td><%= sensor.modulation.name if sensor.modulation %></td>
            <td><%= sensor.room.name if sensor.room %></td>
            <td><%= link_to 'Show', sensor %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    
    <%= link_to 'New Sensor', new_sensor_path %> -->  

