<% provide(:head_title, 'Inscription') %>
<h1 class="center">Inscription</h1>

<%= form_for(@user) do |f| %>
	<%= render 'fields', f: f %>
	<%= f.submit "S'inscrire", class: "btn btn-large btn-success" %>
<% end %>

<p><%= link_to "Déjà inscrit : se connecter", signin_path %></p>
